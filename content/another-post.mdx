---
title: "Another Post"
description: A follow-up post that continues the developer journal, notes, links, and brief examples that match the style used across the other posts.
date: 2025-06-01
tags: ['update', 'journal']
image: /images/blog/first-post.png
author: You
---

# Another Post

This is a follow-up entry that matches the simple post design used elsewhere in the project. Use this file as a template for additional short posts or journal-style updates.

## Quick Notes

- Built a minimal example to match the project's existing post layout.
- Keep frontmatter consistent: `title`, `description`, `date`, `tags`, `image`, `author`.

## Example Code

Inline `code` example: `console.log('hello')`

```js
export function greet(name) {
  return `Hello, ${name}!`
}

console.log(greet('friend'))
```

Thanks for reading — more posts coming soon.

```
